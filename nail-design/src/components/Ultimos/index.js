import styles from "./Ultimos.module.css"

function Ultimos() {
    return(
        <section className={styles.ultimos}>
            <h2>ÚLTIMOS TRABALHOS</h2>

        </section>
    );
}

export default Ultimos;